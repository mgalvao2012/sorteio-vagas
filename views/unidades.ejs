<!DOCTYPE html>
<html>
<head>
<title>Sorteio de vagas de estacionamento do Condomínio Grandlife Ipiranga</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<link href="./css/style.css" rel='stylesheet' type='text/css' />
<link href='./css/notify.min.css' rel='stylesheet' type='text/css' />
<link href='./css/sticky.css' rel='stylesheet' type='text/css' />
<link rel="icon" type="image/png" href="./images/sve.png">
</head>
<body>
<%- include('navbar.ejs') %>

<!-- Form Section -->
  <div class="w3-container w3-light-grey" style="padding:78px 16px;" id="sorteio">
    <div style="padding: 10px; box-shadow: 0 3px 18px 8px rgb(38 24 24 / 25%);">
      <h3 class="w3-center">Configuração das unidades</h3>
      <p class="w3-center w3-medium">Confirme as informações abaixo de cada unidade.<br/>
      Clicando <span class="fa fa-hand-pointer-o icon-hand-pointer"></span> nos ícones é possível atualizar as condições que irão influenciar no sorteio.</p>      
      <div style="margin-top:28px; width: 90%; margin: auto;">
        <div>
            <form>
              <div>
                <div class="w3-bar" >
                  <input id="S1b" type="button" class="aba-subsolo aba-subsolo-foco" value="Torre 1">
                  <input id="S2b" type="button" class="aba-subsolo" value="Torre 2">
                  <script>
                    document.getElementById('S1b').addEventListener('click', function() { abreSubsolo('S1') })                      
                    document.getElementById('S2b').addEventListener('click', function() { abreSubsolo('S2') })                      
                  </script>
                </div> 
                <div id="S1" class="cards-unidades subsolo w3-border">  
                    <% lista_unidades.forEach(function(row) { %>                          
                        <% if (row.unidade.substring(0,2) == 'T1' ) { %>
                          <div>
                            <span style="font-weight: bold;">
                              <%- row.unidade.substring(2) %>
                            </span>-
                            <% if(row.pne) { %>                              
                              <img id="<%- row.unidade %>-pne-icon" src="../images/icon-pne-ok.png" class="icon-hand-pointer"></img>
                            <% } else { %>
                              <img id="<%- row.unidade %>-pne-icon" src="../images/icon-pne-no-ok.png" class="icon-hand-pointer"></img>
                            <% } %> 
                            -
                            <% if(row.adimplente) { %>
                              <img id="<%- row.unidade %>-adimplente-icon" src="../images/icon-adimplente-ok.png" class="icon-hand-pointer"></img>
                            <% } else { %>
                              <img id="<%- row.unidade %>-adimplente-icon" src="../images/icon-adimplente-no-ok.png" class="icon-hand-pointer"></img>
                            <% } %>
                            -
                            <% if (row.user_id != undefined) { %>
                              <span id="<%- row.unidade %>-icon" class="fa fa-user icon-color-ok"></span>
                            <% } else { %>
                              <span id="<%- row.unidade %>-icon" class="fa fa-user icon-color-no-ok"></span>
                            <% } %>                            
                            <input id="<%- row.unidade %>-userid" type="hidden" value="<%- row.user_id %>" />
                          </div>
                        <% } %>
                    <% }) %>
                </div>
                <div id="S2" class="cards-sorteio subsolo w3-border" style="display:none">                     
                  <% lista_unidades.forEach(function(row) {
                      if (row.unidade.substring(0,2) == 'T2' ) { %>
                        <div class="card-sorteio">
                          <span style="font-weight: bold;">
                            <%- row.unidade.substring(2) %>
                          </span>-
                          <% if(row.pne) { %>                              
                            <img id="<%- row.unidade %>-pne-icon" src="../images/icon-pne-ok.png" class="icon-hand-pointer"></img>
                          <% } else { %>
                            <img id="<%- row.unidade %>-pne-icon" src="../images/icon-pne-no-ok.png" class="icon-hand-pointer"></img>
                          <% } %> 
                          -
                          <% if(row.adimplente) { %>
                            <img id="<%- row.unidade %>-adimplente-icon" src="../images/icon-adimplente-ok.png" class="icon-hand-pointer"></img>
                          <% } else { %>
                            <img id="<%- row.unidade %>-adimplente-icon" src="../images/icon-adimplente-no-ok.png" class="icon-hand-pointer"></img>
                          <% } %>
                          -
                            <% if (row.user_id != undefined) { %>
                              <span id="<%- row.unidade %>-icon" class="fa fa-user icon-color-ok"></span>
                            <% } else { %>
                              <span id="<%- row.unidade %>-icon" class="fa fa-user icon-color-no-ok"></span>
                            <% } %>                            
                            <input id="<%- row.unidade %>-userid" type="hidden" value="<%- row.user_id %>" />
                        </div>
                  <% } %>
                  <% }) %>
                </div>
              </div>
              <div class="w3-row w3-medium">
                <div class="w3-col m9 l9">
                  <label class="w3-label">
                    Formato: Unidade - Portador de Necessidades Especiais ( <img src="../images/icon-pne.png" width="14px;"/> ) - Adimplente ( <img src="../images/icon-adimplente.png" width="18px;"/> )
                  </label> 
                </div>
                <div class="w3-col m3 l3">
                  Legenda:
                  <img src="../images/square-green.png" width="14px" height="14px"> Sim&nbsp;&nbsp;&nbsp;
                  <img src="../images/square-red.png" width="14px" height="14px"> Não
                </div>
              </div>              
            </form>
        </div>
      </div>
    </div>
  </div>  
  <!-- Footer -->
  <footer class="w3-center w3-black w3-padding-24">
    <a href="#sorteio" class="w3-button w3-light-grey"><i class="fa fa-arrow-up w3-margin-right"></i>Vai para o início</a>
    <p>Powered by <a href="https://www.w3schools.com/w3css/default.asp" title="W3.CSS" target="_blank" class="w3-hover-text-green">w3.css</a></p>
  </footer>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
  <script src="./js/notify.js"></script>  
  <style>  
    /* The Modal (background) */
    .modal {
      display: none; /* Hidden by default */
      position: fixed; /* Stay in place */
      z-index: 1; /* Sit on top */
      padding-top: 100px; /* Location of the box */
      left: 0;
      top: 0;
      width: 100%; /* Full width */
      height: 100%; /* Full height */
      overflow: auto; /* Enable scroll if needed */
      background-color: rgb(0,0,0); /* Fallback color */
      background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
    }
  
    /* Modal Content */
    .modal-content {
      background-color: #fefefe;
      margin: auto;
      padding: 20px;
      border: 1px solid #888;
      width: 80%;
    }
  
    /* The Close Button */
    .close {
      color: #aaaaaa;
      float: right;
      font-size: 28px;
      font-weight: bold;
    }
  
    .close:hover,
    .close:focus {
      color: #000;
      text-decoration: none;
      cursor: pointer;
    }
  </style>
  <!-- The Modal -->
  <div id="myModal" class="modal">
    <!-- Modal content -->
    <div class="modal-content">
      <span class="close">&times;</span>
      <p id="myModalMensagem"></p>
      <p>
        <button id="btnConfirmar" class="w3-button w3-black" type="button">
            <i class="fa fa-paper-plane"></i> CONFIRMAR
        </button>
        <button id="btnCancelar" class="w3-button w3-black" type="button">
          <i class="fa fa-times-circle"></i> CANCELAR
        </button>
      </p>
    </div>
  </div>

  <script>
    // Get the modal
    var modal = document.getElementById("myModal");

    // Get the <span> element that closes the modal
    var span = document.getElementsByClassName("close")[0];

    // When the user clicks on <span> (x), close the modal
    span.onclick = function() {
      modal.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
      if (event.target == modal) {
        modal.style.display = "none";
      }
    }
  </script>
  <script> 
    var lista_unidades = '<%= lista_unidades %>'
    var lista_ajustada_unidades = '<%= lista_ajustada_unidades %>'.split(',')
    var resultado_sorteio = '<%= resultado_sorteio %>'
    var lista_pne = []
    var lista_adimplente= []

    const notify = new Notify();
    var estilo = '<%= (mensagem != null ? mensagem[0] : "") %>';
    var titulo = '<%= (mensagem != null ? mensagem[1] : "") %>';
    var mensagem = '<%= (mensagem != null ? mensagem[2] : "") %>';
    if (mensagem != '') {
        notify.render({
        head: titulo,
        style: estilo,
        content: mensagem
      });
    }

    // atualiza as unidades com o status de presença gravado no banco de dados
    function loadFunction() { 

      if(resultado_sorteio == 'Sorteio não realizado') {
        for(var i=0; i<lista_ajustada_unidades.length; i++) {
          let unidade = lista_ajustada_unidades[i].split('-')[0]
          if (lista_ajustada_unidades[i].split('-')[1] == 'true') {
            lista_pne.push(unidade)
          }
          if (lista_ajustada_unidades[i].split('-')[2] == 'true') {
            lista_adimplente.push(unidade)
          }
          // adiciona o evento onclick aos icones
          document.getElementById(unidade+'-pne-icon').addEventListener('click', 
            function() { unidadeClicadaPNE(unidade) })
          document.getElementById(unidade+'-adimplente-icon').addEventListener('click', 
            function() { unidadeClicadaAdimplente(unidade) })
          document.getElementById(unidade+'-icon').addEventListener('click', 
            function() { unidadeClicada(unidade) })
        }  
      }
    }
    window.onload = loadFunction();

    function unidadeClicada(valor) {  
      let user_id = document.getElementById(valor+'-userid').value;
      if (user_id != '') {
        document.getElementById('myModal').style.display='block';
        document.getElementById('btnConfirmar').addEventListener('click',
          function() { liberarUnidade(valor) });
        document.getElementById('btnCancelar').addEventListener('click',
          function() { document.getElementById('myModal').style.display='none' });
        document.getElementById('myModalMensagem').innerHTML = 
          'Você tem certeza que deseja cancelar o registro da unidade '+valor+' ? '+
          'Os grupos de vagas escolhidos serão perdidos!';
      }
    }

    function liberarUnidade(valor) {
      document.getElementById('myModal').style.display='none';
      $(document).ready(function(){
        $(document).ajaxError(function(e, xhr, opt) {
          notify.render({
            head: 'Atenção!',
            style: 'error',
            content: xhr.responseJSON.message
          })
        });
        $.post("/unidades/liberarUnidade",
          {
            "unidade": valor
          },
          function(data,status){
            let element = document.getElementById(valor+'-icon');
            element.classList.remove('icon-color-ok');
            element.classList.add('icon-color-no-ok');
            if( data.status == 'success') {
              notify.render({
                head: 'Informação',
                style: 'success',
                content: 'Unidade '+valor+' foi liberada com sucesso!'
              });
            } else {
              notify.render({
                head: 'Atenção',
                style: 'error',
                content: 'Unidade '+valor+'. Erro: '+data.message
              });
            }
          }
        );
      });  
    }

    function unidadeClicadaPNE(valor) {  
      var posicao = lista_pne.indexOf(valor);
      if (posicao > -1) {
        $(document).ready(function(){
          $(document).ajaxError(function(e, xhr, opt) {
            notify.render({
              head: 'Atenção!',
              style: 'error',
              content: xhr.responseJSON.message
            })
          });
          $.post("/unidades/atualizarPNE",
            {
              "unidade": valor,
              "pne": "false"
            },
            function(data,status){
              let element = document.getElementById(valor+'-pne-icon');
              element.src = '../images/icon-pne-no-ok.png'
              lista_pne.splice(posicao,1);
              if( data.status == 'success') {
                notify.render({
                  head: 'Informação',
                  style: 'warning',
                  content: 'Unidade '+valor+' não possui PNE!'
                });
              } else {
                notify.render({
                  head: 'Atenção',
                  style: 'error',
                  content: 'Unidade '+valor+'. Erro: '+data.message
                });
              }
            }
          );
        });    
      } else {
        $(document).ready(function(){
          $(document).ajaxError(function(e, xhr, opt) {
            notify.render({
              head: 'Atenção!',
              style: 'error',
              content: xhr.responseJSON.message
            })
          });
          $.post("/unidades/atualizarPNE",
            {
              "unidade": valor,
              "pne": "true"
            },
            function(data,status){
              let element = document.getElementById(valor+'-pne-icon');
              element.src = '../images/icon-pne-ok.png'
              lista_pne.push(valor);
              if( data.status == 'success') {
                notify.render({
                  head: 'Informação',
                  style: 'success',
                  content: 'Unidade '+valor+' possui PNE!'
                });
              } else {
                notify.render({
                  head: 'Atenção',
                  style: 'error',
                  content: 'Unidade '+valor+'. Erro: '+data.message
                });
              }
            }
          );
        });
      }
    }

    function unidadeClicadaAdimplente(valor) {  
      var posicao = lista_adimplente.indexOf(valor);
      if (posicao > -1) {
        $(document).ready(function(){
          $(document).ajaxError(function(e, xhr, opt) {
            notify.render({
              head: 'Atenção!',
              style: 'error',
              content: xhr.responseJSON.message
            })
          });
          $.post("/unidades/atualizarAdimplente",
            {
              "unidade": valor,
              "adimplente": "false"
            },
            function(data,status){
              let element = document.getElementById(valor+'-adimplente-icon');
              element.src = '../images/icon-adimplente-no-ok.png'
              lista_adimplente.splice(posicao,1);
              if( data.status == 'success') {
                notify.render({
                  head: 'Atenção',
                  style: 'warning',
                  content: 'Unidade '+valor+' está INADIMPLENTE!'
                });
              } else {
                notify.render({
                  head: 'Atenção',
                  style: 'error',
                  content: 'Unidade '+valor+'. Erro: '+data.message
                });
              }
            }
          );
        });    
      } else {
        $(document).ready(function(){
          $(document).ajaxError(function(e, xhr, opt) {
            notify.render({
              head: 'Atenção!',
              style: 'error',
              content: xhr.responseJSON.message
            })
          });
          $.post("/unidades/atualizarAdimplente",
            {
              "unidade": valor,
              "adimplente": "true"
            },
            function(data,status){
              let element = document.getElementById(valor+'-adimplente-icon');
              element.src = '../images/icon-adimplente-ok.png'
              lista_adimplente.push(valor);
              if( data.status == 'success') {
                notify.render({
                  head: 'Informação',
                  style: 'success',
                  content: 'Unidade '+valor+' está ADIMPLENTE!'
                });
              } else {
                notify.render({
                  head: 'Atenção',
                  style: 'error',
                  content: 'Unidade '+valor+'. Erro: '+data.message
                });
              }
            }
          );
        });
      }
    }

    function abreSubsolo(subsolo) { 
      var i;
      var x = document.getElementsByClassName("subsolo");
      for (i = 0; i < x.length; i++) {
        x[i].style.display = "none";  
      }
      x = document.getElementsByClassName("aba-subsolo");
      for (i = 0; i < x.length; i++) {
        x[i].classList.remove("aba-subsolo-foco"); 
      }    
      var element = document.getElementById(subsolo+"b");
      element.classList.add("aba-subsolo-foco");
      if(subsolo == 'S1' || subsolo == 'S2') {
        document.getElementById(subsolo).style.display = "grid";  
      } else {
        document.getElementById(subsolo).style.display = "block";  
      }   
    }

    // Get the modal
    var modal = document.getElementById('id01');

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
      if (event.target == modal) {
        modal.style.display = "none";
      }
    }
  </script>
  <script src="./js/script.js"></script>
  </body>
</html>
  